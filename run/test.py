# Основний тестовий скрипт - використовує фінальний тест
import sys
import os

# Додаємо поточну директорію до шляху для імпорту
sys.path.append(os.path.dirname(os.path.abspath(__file__)))

def main():
    """Головна функція - перенаправляє на фінальний тест"""
    print("\n" + "=" * 60)
    print("🧪 ОСНОВНИЙ ТЕСТОВИЙ СКРИПТ")
    print("=" * 60)
    
    print("\n📋 Цей скрипт перенаправляє на фінальний тест.")
    print("   Будь ласка, використовуйте test_final.py")
    print("\n   Запустіть команду:")
    print("   python test_final.py")
    
    print("\n📌 Особливості фінального тесту:")
    print("   1. 7 різних методів копіювання тексту")
    print("   2. Вибір конкретного методу для тестування")
    print("   3. Натисніть Enter → 10-секундний відлік")
    print("   4. Автоматичний пошук поля по зображенню test.png")
    print("   5. Тестування одного методу на знайдених координатах")
    
    print("\n🚀 Запускаю фінальний тест...")
    
    try:
        # Імпортуємо та запускаємо фінальний тест
        from test_final import main as final_main
        final_main()
    except ImportError as e:
        print(f"\n❌ Помилка імпорту: {e}")
        print("   Переконайтеся, що файл test_final.py існує")
    except Exception as e:
        print(f"\n❌ Помилка: {e}")

if __name__ == "__main__":
    main()
